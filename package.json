{
  "name": "portfolio",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "license": "MIT",
  "engines": {
    "node": "*"
  },
  "scripts": {
    "compileAll": "cd packages/components && yarn compile",
    "buildForNetlify": "cd packages/web && yarn build:production"
  }
}
